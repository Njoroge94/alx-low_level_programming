#include <unistd.h>
#include <string.h>
#include <stdio.h>
#include "holberton.h"
/**
 *_strcpy - copying a string to another
 *@dest: the destination of the copied string
 *@src: the source of string to be copied
 *Return: Always 0 (success)
 */
char *_strcpy(char *dest, char *src)
{
	/*memset(dest, '\0', sizeof(dest));*/
	_strcpy(src, ptr);
	_strcpy(dest, src);

	printf("%s", dest);

	return (dest);
}

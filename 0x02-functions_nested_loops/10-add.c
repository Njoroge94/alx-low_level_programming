#include <unistd.h>
#include "holberton.h"
/**
 *add - the add prototype
 *@r: the number
 *@n: the digit
 *Return: Always 0 (success);
 */
int add(int r, int n)
{
	int sum;

	sum = r + n;
	return (sum);
}
